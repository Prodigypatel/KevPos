# Increase max connections for handling multiple transactions
max_connections = 200

# Shared memory buffers to improve read performance
shared_buffers = 512MB

# Cache optimization for repeated queries
effective_cache_size = 2GB

# Write-ahead log settings for better performance
wal_level = minimal
synchronous_commit = off
checkpoint_timeout = 10min
